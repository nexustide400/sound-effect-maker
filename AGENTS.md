# 効果音の試聴ページを作るエージェント

このフォルダは、音のイメージから複数の効果音を作り、ブラウザーで聴き比べて選ぶための作業場所です。
配布先の利用者は `wishes.txt` をメモ帳などで編集し、Codexデスクトップアプリでこのフォルダを開き、`よろしく` と伝えます。CodexがHTML生成から内蔵ブラウザーでの試聴ページ表示まで担当します。
このAGENTS.mdはエージェント用です。利用者にMarkdownの編集を求めないでください。

## 依頼の読み取り

- 毎回、同じフォルダの `wishes.txt` を読んで制作する。自然文や箇条書きでよく、厳密な書式は要求しない。
- 記入例のまま実行された場合は、その内容で制作する。利用者が書いた依頼を勝手に上書きしない。
- txtはUTF-8を標準とする。古いメモ帳で保存されたファイルがUTF-8で読めない場合はCP932も確認し、文字化けしたまま推測しない。
- 配布物は `AGENTS.md`、`wishes.txt`、`README.md` で構成できる。完成ページは単独でも配布できるようにする。
- 制作時にはファイルを作成できるAIエージェント環境が必要。txt単体がプログラムとして実行されるとは説明しない。

## 実行するタイミング

- `よろしく`、`do it`、`作って`、`生成して` などの指示を受けたら、今回の依頼を読み、動く試聴ページを完成させ、Codex内蔵ブラウザーで開く。HTMLの作成やファイルリンクの提示だけで完了にしない。
- このMarkdown自体の作成・編集・相談を依頼された場合は、その作業だけを行う。指示書を読んだだけで音の生成を始めない。
- 会話で指定された変更や条件を優先する。依頼欄が記入例のままで実行を頼まれた場合は、この例で制作する。
- 些細な不足は用途から補い、採用した前提を短く伝えて進める。必要な音や用途がまったく判断できない場合だけ質問する。

## 完成させるもの

標準の出力先はこのフォルダ内の `output/`。既存の構成がある場合はそれを尊重する。

```text
output/
  sound-candidates.html       試聴ページ
```

- CSS、通常のJavaScript、候補データ、必要な素材をHTML内に含め、単一HTMLとして完成させる。外部CDN、fetchによるファイル読込、ES Modules、サービスワーカー、サーバーAPIに依存しない。
- 主な利用経路はCodex内蔵ブラウザーのHTTPプレビュー。Codexがローカルサーバーの起動とブラウザー表示を行う。利用者にPython、Node、npm、ビルダー、サーバーの導入やコマンド入力、HTMLの手動起動を通常の手順として求めない。
- 単一HTMLはChromeやEdgeで直接開いて試聴・WAV保存できる構成にする。ページ自体にサーバーは不要。内蔵ブラウザーのHTTPプレビューと外部ブラウザーの直接起動をREADMEで説明する。
- 利用者がChromeなどでの試聴を希望した場合はその指定を優先し、内蔵ブラウザーやローカルサーバーを必須にしない。HTMLを作成・更新し、可能なら指定ブラウザーで開く。開けない環境なら同じHTMLを開く手順を案内する。既に外部ブラウザーで開いている場合は再読み込みを案内する。音の修正指示は引き続きCodexで受ける。
- ページだけ、ダミーのボタンだけ、説明だけで完了にしない。すべての候補を実際に試聴し、WAVとして保存できる状態にする。
- 企画名、場面、候補数、説明文は今回の依頼から作る。特定のゲーム名・画像・音源・ローカルパスを前提にしない。
- 合計候補数や表示ラベルはデータから求める。候補数を固定の文字列で埋め込まない。
- 既存のゲームやアプリへの音の組み込みは別の依頼として扱う。この作業では比較して選べるところまで完成させる。

## 音を設計する

1. 各場面について、役割、質感、立ち上がり、音程の動き、長さ、余韻、連続再生時の聴こえ方を決める。
2. 指定がなければ1場面につき6候補。単なる音量違いや音程違いで埋めず、音色・リズム・材質感・余韻などを変えて比較できる候補にする。依頼された世界観は共通に保つ。
3. 候補に短い日本語名と、違いが分かる一文を付ける。「案1」だけで済ませない。
4. 基本は外部サービスや追加依存を必要としないWeb Audioによる合成を使う。発振器、ノイズ、フィルター、音量包絡、倍音、短い旋律などを組み合わせる。
5. 実録の声や自然音など、合成では質感を十分再現できない要求は、その限界を伝える。近似した音を本物の録音と説明しない。外部素材や有料サービスが必要なら、利用可能なものとユーザーの指定に従う。
6. `OfflineAudioContext` などで候補ごとの音声バッファを作り、試聴とWAV保存で同じ音声を使用する。ノイズは固定シードにして再生成可能にする。
7. 先頭と末尾に適切なフェードを設け、クリックや余韻の不自然な切断を防ぐ。全サンプルが有限値で、無音ではなく、クリップしないことを確認する。音量だけで良く聴こえる比較にならないよう候補間のレベルを調整する。
8. プレビュー音量は初期値を控えめにし、生成音そのもののレベルと分離する。音量スライダーでWAVファイルの音量が変わらないことをUIに短く明記する。

## 試聴ページの標準機能

- タイトルは `Sound Effect Maker`。Soundだけ緑色（#54866a）にし、Georgiaを優先するセリフ体で表示する。外部フォントの読込は不要。その下に「ブラウザーの中で合成するオリジナル効果音。WAVは44.1kHz・16bit・モノラル。ネット接続は不要です。」を表示する。生成方式や形式が変わる場合は実装に合わせる。「WAVは」の前で改行する。追加の紹介文や末尾の重複説明は不要。
- タイトルのすぐ右に、音符などのオブジェを選んでいるSDエルフのイラストを配置する。ページ右端に離さず、タイトルと同じ行に並べ、説明文はその行の下に置く。明るくつやのあるアニメ調の既存画像 assets/elf-sound-picker-v2.png を再利用し、HTML内に埋め込んで単一ファイルで表示可能にする。狭い画面では縮小または折り返して文字と重ならないようにする。
- 全件・場面別・お気に入りで絞り込み。
- 場面別の候補カード。各カードの操作は「再生」「☆（お気に入り）」「WAV保存」の3つ。
- 音量スライダーと数値、再生中の候補が分かる表示。
- お気に入りは複数選択できる。localStorageを利用できる場合にお気に入りと音量を保存する。保存できない環境でも試聴を継続する。
- 停止ボタン、単音3回試聴、順番再生、採用候補の選択・一覧、候補メモの入出力は設けない。
- スマートフォンでも操作できるカード配置。ボタンのラベル、キーボード操作、フォーカス表示を設ける。通常の再生開始・完了・保存成功メッセージやその空白は表示せず、エラー時だけ通知する。

## 再生と保存の品質

- 自動再生しない。ユーザーが再生を押した後でAudioContextを開始または再開する。
- 新しい再生操作は前の再生を止め、音が重ならないようにする。停止処理は内部で行い、停止ボタンは表示しない。
- 音の生成中に別候補の再生をしても、古い処理が後から鳴らないようにする。ページを離れるときも停止する。
- フィルターやお気に入りを操作した後も、再生状態と表示を整合させる。
- WAVはPCM 16bit、44.1kHz以上を標準にし、実際のチャンネル数・サンプルレートと一致したヘッダーを作る。ファイル名には企画・場面・安定した候補IDを含める。
- 保存データは読み込み時に型と候補IDを検証する。削除された候補は無視する。並び替えで別の音に選択が移らないよう、配列位置を永続IDにしない。
- 候補の音を大きく変更する場合は新しいIDまたは版を割り当て、過去の採用状態を黙って別の音に引き継がない。
- エラーは画面に短く表示し、再試行できる状態にする。

## 制作から起動までの進め方

1. フォルダ内の既存ファイルを調べ、ユーザーの作業を保ったまま作成・更新する。以前のページから着想を得ても、そのページへの依存は持ち込まない。
2. 依頼に合わせた音のデータ、合成処理、ページを実装する。既存の実装が使える場合は必要な部分を更新する。
3. 利用可能なブラウザーや検証ツールでページを確認する。全候補の生成、有限値・ピーク・無音の検査、単音再生、再生切り替え、お気に入りの永続化、WAV保存を確認する。代表的なWAVのヘッダーと実データの整合も調べる。
4. 実際に音を聴ける環境なら代表候補を聴いて調整する。波形検査や再生APIの成功だけで「聴感確認済み」とは報告しない。実行できなかった確認は記録する。
5. Codexで使えるプレビュー機能と実行環境を調べる。ワークスペースの同梱ランタイムを取得するツール（例：load_workspace_dependencies）があれば優先する。利用者のPCにPythonやNodeが入っているとは仮定しない。端末固有の絶対パスを指示書に固定しない。
6. 利用できるランタイムで、outputだけを配信するローカルHTTPサーバーを起動する。Nodeを使う場合は標準モジュールだけで実装でき、npmインストールは不要。127.0.0.1のみにバインドし、既存の別サイトを停止しない。同じ出力を配信しているサーバーは再利用し、ポートが競合する場合は空きポートを使う。パスの逸脱や作業フォルダ全体の公開を防ぐ。
7. HTMLへのHTTPアクセスが成功することを確認する。そのURLをCodex内蔵ブラウザーで開くツール（例：open_in_codexのbrowserターゲット、または利用可能な内蔵ブラウザー操作ツール）に渡す。内蔵ブラウザーを希望している場合、外部ブラウザーの起動やファイルリンクの提示で代用しない。外部ブラウザーが明示された場合は、その指定に従い手順5〜8のHTTPプレビュー起動を省略できる。Windowsでバックグラウンド起動する場合は非表示にする。
8. 利用可能なブラウザー操作でページが表示されたことを確認し、再生、再生切り替え、お気に入り、WAV保存などを検証する。表示要求がqueuedの場合は表示確認済みとしない。機能・権限が足りない場合は正確な制約を伝え、生成済みHTMLは残す。セキュリティ制限を回避しない。別途インストールが必要になった場合も、黙って追加せず具体的な不足を伝える。
9. 検証結果と未確認事項はチャットで報告する。制作メモや作業ログを配布用フォルダに追加しない。端末固有の起動方法・プロセス・停止方法が必要ならチャットで案内する。
10. 最後に、場面数・候補数とプレビューURLを短く報告する。通常の完了状態は「利用者がCodex内蔵ブラウザーですぐ試聴できる」。外部ブラウザー指定時は、そのブラウザーで更新後のHTMLを試聴できる状態を目指す。再度開く依頼でも、Codexがサーバーの状態を確認してブラウザー表示まで行う。

## 追加調整の依頼を受けたら

「もっと柔らかく」「この場面だけ増やして」「3番の余韻を短く」のような指示では、対象の候補を特定して音と説明を更新する。関係のない候補やお気に入りを作り直さない。必要な検証とページの更新を行い、変更した候補を伝える。
